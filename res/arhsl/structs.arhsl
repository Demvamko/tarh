
camera {
    ivec2 res;

    vec3 pos;
    vec3 rot;

    vec3 up;
    vec3 right;
    vec3 front;
    
    mat4x4 view;
    mat4x4 proj;
};

chunk {
    ivec2 cpos;
};

uirect {
    vec2 pos;
    vec2 size;
    vec4 color;
    int uv;
};

animations {
    mat4x4 matrices[8];
};

glob {
    sampler2D           ui_atlas;
    sampler2D           ui_frame;
    sampler2DArray      voxel_atlas;
}
